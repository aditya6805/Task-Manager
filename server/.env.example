# Server
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=

# CORS
# Comma-separated allowed origins
CORS_ORIGIN=http://localhost:5173

# Firebase Admin
# Option 1: keep config/serviceAccountKey.json locally (development)
# Option 2 (recommended for deployment): provide JSON string below
FIREBASE_SERVICE_ACCOUNT=
